{
  "name": "seagm-price-monitor",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "trigger": "wrangler dev --test-scheduled"
  },
  "devDependencies": {
    "wrangler": "^4.87.0"
  }
}
